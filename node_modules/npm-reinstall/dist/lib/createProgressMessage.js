"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var chalk_1 = __importDefault(require("chalk"));
var cyan = chalk_1.default.cyan, green = chalk_1.default.green, red = chalk_1.default.red;
var typeTexts = Object.freeze({
    GLOBAL: 'globally',
    SAVE: 'dependencies',
    SAVE_DEV: 'devDependencies'
});
function createProgressMessage(progress, mode, packages) {
    if (progress === 'done') {
        return green('Finish reinstallation.');
    }
    if (progress === 'fail') {
        return red('Reinstallation fail.');
    }
    // Is for all packages?
    var isForAll = !mode ||
        !packages ||
        (Array.isArray(packages) && packages.length === 0);
    var progressMsg = progress === 'install' ? 'Installing' : 'Uninstalling';
    var packagesStr = isForAll
        ? 'all packages'
        : cyan.bold(packages.join(' '));
    var prep = progress === 'install' ? 'as' : 'from';
    var type = typeTexts[mode];
    var msgs = [progressMsg, packagesStr];
    if (!isForAll) {
        msgs.push(prep);
        msgs.push(type);
    }
    msgs.push('...');
    return msgs.join(' ');
}
exports.default = createProgressMessage;
