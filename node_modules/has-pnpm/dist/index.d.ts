/**
 * Check if a project is using pnpm.
 *
 * @param {string} [cwd=process.cwd()] Current working directory
 * @returns {boolean}
 */
declare function hasPNPM(cwd?: string): boolean;
export = hasPNPM;
