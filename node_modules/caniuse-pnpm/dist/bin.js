#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
const figures_1 = require("figures");
const os_1 = require("os");
const yargs_1 = __importDefault(require("yargs"));
const index_1 = __importDefault(require("./index"));
const { cyan, green, red } = chalk_1.default;
const pnpm = cyan('pnpm');
// tslint:disable:no-unused-expression
yargs_1.default
    .scriptName(cyan('caniuse-pnpm'))
    .help()
    .version()
    .alias('help', 'h')
    .alias('version', 'v')
    .usage('$0', 'Check if pnpm is available.')
    .argv;
// tslint:enable:no-unused-expression
if (index_1.default()) {
    process.stdout.write(green(`${figures_1.tick} ${pnpm} is available. You can use ${pnpm}.${os_1.EOL}`));
}
else {
    process.stdout.write(red(`${figures_1.cross} ${pnpm} is not available! You cannot use ${pnpm}.${os_1.EOL}`));
}
